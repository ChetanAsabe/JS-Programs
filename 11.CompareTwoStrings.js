const string1 = "Chetan";
const string2 = "chetan";

const reseult = string1.toLowerCase() === string2.toLowerCase();

if(reseult) 
    console.log('Two Strings are Similar.');
else
    console.log('Two Strings are not Similar.');